import { c as _export_sfc, Q as openBlock, j as createElementBlock, m as createStaticVNode } from "./chunks/framework.OPxAocOb.js";
const __pageData = JSON.parse('{"title":"AI Agents","description":"","frontmatter":{},"headers":[],"relativePath":"agents.md","filePath":"agents.md"}');
const _sfc_main = { name: "agents.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="ai-agents" tabindex="-1">AI Agents <a class="header-anchor" href="#ai-agents" aria-label="Permalink to &quot;AI Agents&quot;">​</a></h1><p>aglang is an <strong>auditable architecture verification layer</strong> that agents can consume while they work. AI coding agents are a primary workflow because they benefit from machine-readable architecture truth, but the same checked <code>.ag</code>, <code>.agq.yml</code>, <code>architecture.o</code>, and JSON verdicts are useful to engineers and CI.</p><h2 id="why-agents-need-architectural-guardrails" tabindex="-1">Why agents need architectural guardrails <a class="header-anchor" href="#why-agents-need-architectural-guardrails" aria-label="Permalink to &quot;Why agents need architectural guardrails&quot;">​</a></h2><p>When an AI agent refactors code, it may:</p><ul><li>Introduce a direct connection between tiers that should be isolated</li><li>Add a database call in a public-facing handler</li><li>Bypass an auth layer &quot;for simplicity&quot;</li><li>Inject infrastructure into UI code, create singleton-to-scoped DI bugs, or use <code>IServiceProvider</code> as a service locator</li></ul><p>Traditional code review catches these eventually. aglang catches them while the agent is still coding, then enforces the same rules at commit time with source evidence, deterministic policy checks, and Z3-backed proof details.</p><h2 id="setup-for-agents" tabindex="-1">Setup for agents <a class="header-anchor" href="#setup-for-agents" aria-label="Permalink to &quot;Setup for agents&quot;">​</a></h2><h3 id="_0-install-the-generic-skill-interface" tabindex="-1">0. Install the generic skill interface <a class="header-anchor" href="#_0-install-the-generic-skill-interface" aria-label="Permalink to &quot;0. Install the generic skill interface&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install-agent-skill</span></span></code></pre></div><p>This installs the packaged <code>aglang</code> Codex skill into <code>${CODEX_HOME:-~/.codex}/skills/aglang</code>, so agents know the CLI workflows after npm install. This is generic product knowledge; project-specific rules still come from <code>AGENTS.md</code> and <code>skill.json</code>.</p><p>When installed from npm, aglang also attempts this step automatically during <code>postinstall</code>. Set <code>AGLANG_SKIP_AGENT_SKILL_INSTALL=1</code> to opt out.</p><h3 id="_1-one-shot-bootstrap" tabindex="-1">1. One-shot bootstrap <a class="header-anchor" href="#_1-one-shot-bootstrap" aria-label="Permalink to &quot;1. One-shot bootstrap&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/project</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> MyApp</span></span></code></pre></div><p>This legacy starter command creates:</p><ul><li><code>architecture.ag</code> — the spec (agents should read this)</li><li><code>architecture.o</code> — the compiled artifact</li><li><code>skill.json</code> — agent skill manifest</li></ul><p><code>.ag</code> files are engineer-guided architecture source. Coding agents should not create, edit, regenerate, or compile changes to <code>.ag</code> specs unless the engineer explicitly asks for architecture/spec work, ideally in a planning or design session.</p><p>Semantic query files in <code>.aglang/extractors/*.agq.yml</code> are also reviewed architecture source. They translate deterministic graph facts into domain facts such as state-machine transitions, architecture flows, named operations, value facts, operation before/after facts, or scoped events. Agents may inspect them to understand provenance, but should ask before creating or changing them.</p><p>For agent-native adoption, prefer task packets:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> request-scan</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .aglang/tasks/architecture-discovery.json</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> request-review</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .aglang/tasks/architecture-review.json</span></span></code></pre></div><p>These commands notify the agent what work is requested. The agent performs semantic scanning, proposal, and review; aglc remains the deterministic compiler/checker for approved artifacts.</p><h3 id="_2-emit-agent-context" tabindex="-1">2. Emit agent context <a class="header-anchor" href="#_2-emit-agent-context" aria-label="Permalink to &quot;2. Emit agent context&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> emit-context</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --arch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> architecture.o</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> AGENTS.md</span></span></code></pre></div><p><code>AGENTS.md</code> is a plain-English description of your architecture rules, suitable for any agent&#39;s context window. Commit it to your repo so agents discover it automatically.</p><h3 id="_3-emit-skill-manifest" tabindex="-1">3. Emit skill manifest <a class="header-anchor" href="#_3-emit-skill-manifest" aria-label="Permalink to &quot;3. Emit skill manifest&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> emit-skill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --arch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> architecture.o</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skill.json</span></span></code></pre></div><p><code>skill.json</code> follows the emerging AI skill/tool manifest format. Agents that support it can load architectural constraints, command templates, violation schema fields, query provenance, diff metadata, and solver diagnostics as part of their toolchain.</p><h2 id="continuous-validation-loop" tabindex="-1">Continuous validation loop <a class="header-anchor" href="#continuous-validation-loop" aria-label="Permalink to &quot;Continuous validation loop&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent reads AGENTS.md → edits code → aglc check-file --json</span></span>\n<span class="line"><span>                                  → aglc check --all --json</span></span>\n<span class="line"><span>                                  → aglc explain --violation &lt;id&gt; --json</span></span>\n<span class="line"><span>                                  → CI can run the same gate</span></span>\n<span class="line"><span>                                                        │</span></span>\n<span class="line"><span>                                          Z3 SAT   → pass ✓</span></span>\n<span class="line"><span>                                          Z3 UNSAT → fix reported code ✗</span></span></code></pre></div><h2 id="json-mode-for-programmatic-integration" tabindex="-1">JSON mode for programmatic integration <a class="header-anchor" href="#json-mode-for-programmatic-integration" aria-label="Permalink to &quot;JSON mode for programmatic integration&quot;">​</a></h2><p>All check commands support <code>--json</code> for machine-readable output:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check-file</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --arch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> architecture.o</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/api/gateway/checkout.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --arch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> architecture.o</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aglc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> explain</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --arch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> architecture.o</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --violation</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> viol_4d72958c9c079a2f</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;verdict&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;violation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;rule&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SecureLedger&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;component&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PublicGateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;file&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/api/gateway/checkout.py&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;line&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">44</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Direct flow to LedgerDatabase is denied by invariant SecureLedger&quot;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Agents can parse this JSON and decide how to fix the violation rather than reading terminal output. For blocking verdicts, the stable <code>id</code> should be passed to <code>aglc explain --violation &lt;id&gt; --json</code> to get the deterministic repair-loop explanation.</p><h2 id="workflow-for-agent-managed-projects" tabindex="-1">Workflow for agent-managed projects <a class="header-anchor" href="#workflow-for-agent-managed-projects" aria-label="Permalink to &quot;Workflow for agent-managed projects&quot;">​</a></h2><ol><li><strong>Setup</strong> — an engineer requests <code>aglc request-scan</code>, then an authorized agent proposes architecture artifacts for review.</li><li><strong>Agent reads</strong> — coding agents read <code>AGENTS.md</code> and <code>skill.json</code> before making implementation changes.</li><li><strong>Agent validates while coding</strong> — run <code>aglc check-file --json</code> for focused edits.</li><li><strong>Agent validates before finishing</strong> — run <code>aglc check --all --json</code> for the guarded project.</li><li><strong>Architecture evolves deliberately</strong> — agents ask before changing <code>.ag</code>, <code>.agq.yml</code>, <code>architecture.o</code>, <code>AGENTS.md</code>, <code>skill.json</code>, or generated context.</li></ol><p>When <code>reach_violation</code>, <code>data_policy_violation</code>, or <code>trust_policy_violation</code> entries appear in <code>violations[]</code>, use <code>detected.path</code>, <code>detected.data</code>, and the Z3 proof to remove the forbidden path or add the declared auth/trust boundary the architecture requires.</p><p>When <code>require_flow_violation</code> or <code>require_operation_violation</code> entries appear in <code>violations[]</code>, fix the implementation so the required path or operation placement is satisfied. Operation facts come from reviewed <code>.agq.yml</code> files; do not edit <code>.ag</code> or <code>.agq.yml</code> to satisfy a require violation unless the engineer explicitly approves an architecture/query change.</p><p>When <code>di_violation</code> entries appear in <code>violations[]</code>, fix the implementation dependency graph. Reach-based DI failures may include a transitive <code>detected.path</code>. Do not work around the gate by editing <code>.ag</code> unless the engineer explicitly asks to change the intended architecture.</p><p>When <code>state_machine_violation</code> entries appear in <code>violations[]</code>, use the machine name, transition edge, source evidence, and <code>detected.query</code> provenance to fix the invalid state write. <code>aglc check</code> never calls an LLM; it evaluates committed source, compiled architecture, and reviewed query files.</p><p>When <code>value_policy_violation</code>, <code>operation_policy_violation</code>, or <code>event_policy_violation</code> entries appear in <code>violations[]</code>, use the policy rule, source evidence, and <code>detected.query</code> provenance to fix the contradictory value, pre/postcondition, or missing event precedence in implementation code.</p><p>When <code>solver_diagnostics[]</code> contains <code>unknown</code>, <code>error</code>, or <code>suggested_refactor</code>, treat it as a path-explosion or modeling hotspot. Prefer simplifying the implementation path, state write, or dependency graph before asking to change architecture rules.</p><p>When <code>change_violations[]</code> appear, update the required companion component in the same change. For example, a CLI or package metadata change may require README, CLI reference, or agent skill updates.</p><h2 id="recommended-agents-md-placement" tabindex="-1">Recommended AGENTS.md placement <a class="header-anchor" href="#recommended-agents-md-placement" aria-label="Permalink to &quot;Recommended AGENTS.md placement&quot;">​</a></h2><p>Place <code>AGENTS.md</code> in the project root. Most agent frameworks automatically inject root-level context files into the agent&#39;s system prompt.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>my-project/</span></span>\n<span class="line"><span>├── AGENTS.md          ← agent reads this</span></span>\n<span class="line"><span>├── architecture.ag    ← source of truth (engineer-guided edits)</span></span>\n<span class="line"><span>├── architecture.o     ← compiled artifact (do not edit)</span></span>\n<span class="line"><span>├── skill.json         ← agent skill manifest</span></span>\n<span class="line"><span>└── src/</span></span></code></pre></div><h2 id="architecture-source-changes" tabindex="-1">Architecture Source Changes <a class="header-anchor" href="#architecture-source-changes" aria-label="Permalink to &quot;Architecture Source Changes&quot;">​</a></h2><p>Agents must ask before changing <code>.ag</code>, <code>.agq.yml</code>, <code>architecture.o</code>, <code>AGENTS.md</code>, or <code>skill.json</code> to satisfy a violation. These files encode architecture intent or generated architecture surfaces, so fixes should normally change implementation code unless the engineer explicitly approves an architecture/query update.</p><p>Evidence-backed <code>require</code> rules compile to deny-counterexample checks. Auth, encryption, dependency, and operation facts come from deterministic extractors or reviewed <code>.agq.yml</code> files during <code>check</code>; they are not inferred by LLM calls.</p>', 48)
  ])]);
}
const agents = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  agents as default
};
