import { c as _export_sfc, Q as openBlock, j as createElementBlock, m as createStaticVNode } from "./chunks/framework.OPxAocOb.js";
const __pageData = JSON.parse('{"title":"Language Reference","description":"","frontmatter":{},"headers":[],"relativePath":"guide/language-reference.md","filePath":"guide/language-reference.md"}');
const _sfc_main = { name: "guide/language-reference.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="language-reference" tabindex="-1">Language Reference <a class="header-anchor" href="#language-reference" aria-label="Permalink to &quot;Language Reference&quot;">​</a></h1><p>An <code>.ag</code> file is a sequence of top-level declarations. Order does not matter; the compiler resolves references during type checking.</p><h2 id="enforcement-semantics" tabindex="-1">Enforcement Semantics <a class="header-anchor" href="#enforcement-semantics" aria-label="Permalink to &quot;Enforcement Semantics&quot;">​</a></h2><p>aglang declarations have explicit enforcement levels:</p><table tabindex="0"><thead><tr><th>Declaration</th><th>Level</th><th>Meaning</th></tr></thead><tbody><tr><td><code>invariant deny flow</code></td><td><code>formal_z3</code></td><td>Extracted flow facts are checked against SMT-LIB constraints in Z3.</td></tr><tr><td><code>invariant deny reach</code></td><td><code>formal_z3</code></td><td>Transitive flow reachability is checked in Z3.</td></tr><tr><td><code>invariant deny dataflow</code></td><td><code>formal_z3</code></td><td>Dataflow facts inferred from handled data and extracted reachability are checked in Z3.</td></tr><tr><td><code>data_policy</code></td><td><code>formal_z3</code></td><td>Data classification and jurisdiction rules are checked against propagated data reachability.</td></tr><tr><td><code>trust_policy</code></td><td><code>formal_z3</code></td><td>Trust-boundary auth and classified data boundary rules are checked from extracted facts and declared node metadata.</td></tr><tr><td><code>change_policy</code></td><td><code>formal_z3</code></td><td>Touched-component facts are checked against SMT-LIB implication rules in Z3.</td></tr><tr><td><code>di_policy</code></td><td><code>formal_z3</code></td><td>Definite constructor-injection, lifetime, and service-locator facts are checked in Z3.</td></tr><tr><td><code>machine</code></td><td><code>formal_z3</code></td><td>Extracted transition facts are checked against declared state-machine transitions.</td></tr><tr><td><code>contract</code></td><td><code>deterministic_policy</code></td><td>Route facts are compared against declared implements/consumes contracts.</td></tr><tr><td><code>workflow_policy</code></td><td><code>deterministic_policy</code></td><td>GitHub Actions facts are checked for release, deploy, publish, permission, and step-order rules.</td></tr><tr><td><code>invariant require encryption</code></td><td><code>advisory</code></td><td>Reported as a warning because static extraction does not yet prove encryption.</td></tr><tr><td><code>permission</code></td><td><code>formal_z3</code></td><td>Authorization intent is emitted and can be enforced when extractors produce definite operation and role-check evidence.</td></tr></tbody></table><p><code>formal_z3</code> and <code>deterministic_policy</code> violations block checks. <code>advisory</code> declarations guide agents and docs, but do not block by themselves unless a future extractor/gate promotes them.</p><h2 id="infrastructure-node" tabindex="-1">Infrastructure Node <a class="header-anchor" href="#infrastructure-node" aria-label="Permalink to &quot;Infrastructure Node&quot;">​</a></h2><p>Declares an infrastructure or runtime entity that components run on or interact with.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>node &lt;name&gt; : &lt;node_type&gt; {</span></span>\n<span class="line"><span>  trust: trusted | untrusted | semi_trusted</span></span>\n<span class="line"><span>  protocol: https | grpc | ws       // optional</span></span>\n<span class="line"><span>  auth: none | jwt | oauth2 | api_key // optional</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Example:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>node api_runtime : server { trust: trusted protocol: https }</span></span>\n<span class="line"><span>node ledger_db : postgres { trust: trusted auth: mtls }</span></span></code></pre></div><p>Node types come from the stdlib, including <code>server</code>, <code>ci_runner</code>, <code>package_registry</code>, <code>static_host</code>, <code>release_host</code>, <code>postgres</code>, <code>redis</code>, <code>s3_bucket</code>, and <code>agent_runtime</code>.</p><h2 id="component" tabindex="-1">Component <a class="header-anchor" href="#component" aria-label="Permalink to &quot;Component&quot;">​</a></h2><p>Declares a logical code component and maps it to source files.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>component &lt;Name&gt; {</span></span>\n<span class="line"><span>  runs_on: &lt;node_name&gt;</span></span>\n<span class="line"><span>  paths: &quot;&lt;glob&gt;&quot;</span></span>\n<span class="line"><span>  role: presentation | application | domain | data_access | infrastructure | integration | test // optional</span></span>\n<span class="line"><span>  layer: &lt;LayerName&gt;        // optional</span></span>\n<span class="line"><span>  implements: &lt;ContractName&gt; // optional, comma-separated</span></span>\n<span class="line"><span>  consumes: &lt;ContractName&gt;   // optional, comma-separated</span></span>\n<span class="line"><span>  handles: &lt;DataType&gt;        // optional, comma-separated</span></span>\n<span class="line"><span>  repo: &lt;RepoName&gt;           // optional</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Example:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>component PublicApi {</span></span>\n<span class="line"><span>  runs_on: api_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/api/**/*.ts&quot;</span></span>\n<span class="line"><span>  implements: UsersApi</span></span>\n<span class="line"><span>}</span></span></code></pre></div><h2 id="resource" tabindex="-1">Resource <a class="header-anchor" href="#resource" aria-label="Permalink to &quot;Resource&quot;">​</a></h2><p>Declares an architectural capability that components may access, such as secure storage, local preferences, platform hardware, or external APIs.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>resource &lt;name&gt; : &lt;resource_type&gt; {</span></span>\n<span class="line"><span>  trust: trusted | untrusted | semi_trusted</span></span>\n<span class="line"><span>  protocol: https | grpc | ws          // optional</span></span>\n<span class="line"><span>  auth: none | jwt | oauth2 | api_key  // optional</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Built-in resource types include <code>secure_storage</code>, <code>local_preferences</code>, <code>external_api</code>, <code>local_database</code>, <code>reactive_stream</code>, <code>message_bus</code>, <code>file_system</code>, <code>sensor</code>, and <code>device_hardware</code>.</p><p>Example:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>resource SecureStorage : secure_storage { trust: trusted }</span></span>\n<span class="line"><span>resource LocalPreferences : local_preferences { trust: semi_trusted }</span></span>\n<span class="line"><span>resource ExternalNetwork : external_api { trust: untrusted protocol: https }</span></span></code></pre></div><h2 id="invariant" tabindex="-1">Invariant <a class="header-anchor" href="#invariant" aria-label="Permalink to &quot;Invariant&quot;">​</a></h2><p>Flow invariants declare component or node relationships that must not be violated.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>invariant &lt;Name&gt; {</span></span>\n<span class="line"><span>  deny flow &lt;ComponentOrNode&gt; -&gt; &lt;ComponentOrNode&gt;</span></span>\n<span class="line"><span>  deny reach &lt;ComponentOrNode&gt; -&gt; &lt;ComponentOrNode&gt;</span></span>\n<span class="line"><span>  deny flow role &lt;RoleName&gt; -&gt; resource &lt;ResourceNameOrType&gt;</span></span>\n<span class="line"><span>  deny flow layer &lt;LayerName&gt; -&gt; resource &lt;ResourceNameOrType&gt;</span></span>\n<span class="line"><span>  deny dataflow &lt;DataType&gt; -&gt; &lt;ComponentOrNode&gt;</span></span>\n<span class="line"><span>  require encryption on flow &lt;ComponentOrNode&gt; -&gt; &lt;ComponentOrNode&gt;</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Example:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>invariant Layering {</span></span>\n<span class="line"><span>  deny flow PublicApi -&gt; ledger_db</span></span>\n<span class="line"><span>  deny flow role presentation -&gt; resource secure_storage</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p><code>deny flow</code> is direct-only for compatibility. Use <code>deny reach</code> to block transitive paths such as <code>UI -&gt; Service -&gt; Db</code>. <code>require encryption on flow</code> is currently advisory because extractors do not yet prove encrypted transport.</p><p><code>deny dataflow</code> is also Z3-backed. It blocks when a component that <code>handles</code> a data type can reach the denied target through one or more extracted flows.</p><h2 id="data-metadata-and-policies" tabindex="-1">Data Metadata And Policies <a class="header-anchor" href="#data-metadata-and-policies" aria-label="Permalink to &quot;Data Metadata And Policies&quot;">​</a></h2><p>Data declarations can carry classification and jurisdiction metadata:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data CustomerProfile {</span></span>\n<span class="line"><span>  classification: pii</span></span>\n<span class="line"><span>  jurisdiction: eu</span></span>\n<span class="line"><span>  id: UUID</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p><code>data_policy</code> blocks use those labels over propagated data reachability:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data_policy Privacy {</span></span>\n<span class="line"><span>  deny classification pii -&gt; untrusted</span></span>\n<span class="line"><span>  deny jurisdiction eu -&gt; NonGdprService</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>The first rule blocks classified data reaching any declared entity whose trust metadata is <code>untrusted</code>. The second blocks data with a specific jurisdiction from reaching a named component, node, or resource.</p><h2 id="trust-policy" tabindex="-1">Trust Policy <a class="header-anchor" href="#trust-policy" aria-label="Permalink to &quot;Trust Policy&quot;">​</a></h2><p>Trust policies use <code>trust:</code> and <code>auth:</code> metadata from nodes and resources. Components inherit metadata from their <code>runs_on</code> node.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>trust_policy Boundaries {</span></span>\n<span class="line"><span>  require auth untrusted -&gt; trusted</span></span>\n<span class="line"><span>  deny flow trusted -&gt; untrusted when data pii</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p><code>require auth</code> blocks an extracted path from an untrusted entity to a trusted entity when the target has no declared auth. <code>deny flow ... when data</code> blocks classified data crossing the declared trust boundary.</p><h2 id="contract" tabindex="-1">Contract <a class="header-anchor" href="#contract" aria-label="Permalink to &quot;Contract&quot;">​</a></h2><p>Contracts define interface shapes between components.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>contract UsersApi {</span></span>\n<span class="line"><span>  GET  &quot;/api/users&quot;      -&gt; User[]</span></span>\n<span class="line"><span>  POST &quot;/api/users&quot;      -&gt; User</span></span>\n<span class="line"><span>  query viewer()         -&gt; User</span></span>\n<span class="line"><span>  rpc GetUser(UserId)    -&gt; User</span></span>\n<span class="line"><span>  publishes: &quot;user.created&quot;</span></span>\n<span class="line"><span>  subscribes: &quot;user.deleted&quot;</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Components opt into contract enforcement with <code>implements:</code> or <code>consumes:</code>.</p><h2 id="workflow-policy" tabindex="-1">Workflow Policy <a class="header-anchor" href="#workflow-policy" aria-label="Permalink to &quot;Workflow Policy&quot;">​</a></h2><p><code>workflow_policy</code> blocks enforce GitHub Actions release and deployment safety. Workflow YAML files are modeled as components, and publish/deploy/release targets are modeled as CI/CD nodes.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>node github_actions : ci_runner { trust: trusted }</span></span>\n<span class="line"><span>node npm_registry : package_registry { trust: trusted auth: api_key }</span></span>\n<span class="line"><span>node github_pages : static_host { trust: trusted auth: oauth2 }</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>component ReleaseWorkflow {</span></span>\n<span class="line"><span>  runs_on: github_actions</span></span>\n<span class="line"><span>  paths: &quot;.github/workflows/release.yml&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>workflow_policy ReleaseSafety {</span></span>\n<span class="line"><span>  allow publish ReleaseWorkflow -&gt; npm_registry when tag &quot;v*.*.*&quot;</span></span>\n<span class="line"><span>  deny publish * -&gt; npm_registry when pull_request</span></span>\n<span class="line"><span>  require before ReleaseWorkflow &quot;npm test&quot; -&gt; &quot;npm publish&quot;</span></span>\n<span class="line"><span>  deny permission * contents: write when pull_request</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Supported actions are <code>publish</code>, <code>deploy</code>, and <code>release</code>. Conditions support <code>when tag &quot;&lt;glob&gt;&quot;</code>, <code>when branch &quot;&lt;glob&gt;&quot;</code>, and <code>when pull_request</code>.</p><h2 id="change-policy" tabindex="-1">Change Policy <a class="header-anchor" href="#change-policy" aria-label="Permalink to &quot;Change Policy&quot;">​</a></h2><p><code>change_policy</code> blocks enforce that related components are updated together in the same checked diff.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>change_policy DocsFreshness {</span></span>\n<span class="line"><span>  require touched CliReferenceDocs when touched CliCompiler</span></span>\n<span class="line"><span>  require touched ReadmeDocs when touched CliCompiler</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Semantics: if any staged file maps to the trigger component, at least one staged file must map to the required component. The gate emits Z3-backed <code>change_violations[]</code> when the implication cannot be satisfied.</p><p>Change policies prove that declared surfaces changed together; they do not prove that prose is semantically complete.</p><h2 id="dependency-injection-policy" tabindex="-1">Dependency Injection Policy <a class="header-anchor" href="#dependency-injection-policy" aria-label="Permalink to &quot;Dependency Injection Policy&quot;">​</a></h2><p><code>di_policy</code> blocks enforce dependency injection boundaries when the runtime can extract definite DI facts. The built-in C# extractor currently detects constructor injection, <code>AddSingleton</code> / <code>AddScoped</code> / <code>AddTransient</code> registrations, and <code>IServiceProvider</code> / <code>GetRequiredService&lt;T&gt;</code> service-locator usage.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>component Views {</span></span>\n<span class="line"><span>  runs_on: app_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/**/Views/**/*.xaml.cs&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>component ViewModels {</span></span>\n<span class="line"><span>  runs_on: app_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/**/ViewModels/**/*.cs&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>component BleManager {</span></span>\n<span class="line"><span>  runs_on: app_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/**/Infrastructure/Bluetooth/**/*.cs&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>component Repositories {</span></span>\n<span class="line"><span>  runs_on: app_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/**/Infrastructure/Persistence/**/*.cs&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>component Application {</span></span>\n<span class="line"><span>  runs_on: app_runtime</span></span>\n<span class="line"><span>  paths: &quot;src/**/Application/**/*.cs&quot;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>di_policy DependencyInjection {</span></span>\n<span class="line"><span>  deny inject Views -&gt; BleManager</span></span>\n<span class="line"><span>  deny inject_reach Views -&gt; Repositories</span></span>\n<span class="line"><span>  deny inject ViewModels -&gt; Repositories</span></span>\n<span class="line"><span>  deny lifetime singleton -&gt; scoped</span></span>\n<span class="line"><span>  deny lifetime_reach singleton -&gt; scoped</span></span>\n<span class="line"><span>  deny resolve IServiceProvider from Application</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Semantics:</p><ul><li><code>deny inject A -&gt; B</code> blocks a constructor dependency from component <code>A</code> to component <code>B</code>.</li><li><code>deny inject_reach A -&gt; B</code> blocks a transitive constructor dependency path from component <code>A</code> to component <code>B</code>.</li><li><code>deny lifetime singleton -&gt; scoped</code> blocks a singleton-registered service depending on a scoped-registered service.</li><li><code>deny lifetime_reach singleton -&gt; scoped</code> blocks a transitive lifetime path from singleton to scoped.</li><li><code>deny resolve IServiceProvider from Application</code> blocks service-locator access from <code>Application</code>.</li></ul><p>Each blocking DI fact becomes an SMT assertion such as <code>(assert (Injects Views BleManager))</code>, <code>(assert (InjectReach Views Repositories))</code>, or <code>(assert (LifetimeReach Lifetime__singleton Lifetime__scoped))</code>. The compiled <code>di_policy</code> contributes the opposite implication, so Z3 returns UNSAT and the JSON verdict reports a <code>di_violation</code>.</p><h2 id="state-machine" tabindex="-1">State Machine <a class="header-anchor" href="#state-machine" aria-label="Permalink to &quot;State Machine&quot;">​</a></h2><p>State machines describe allowed transitions for a data type field. They are enforced when deterministic extractor queries emit transition facts from reviewed <code>.agq.yml</code> files.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>enum OrderStatus { Draft | Active | Archived }</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>data Order {</span></span>\n<span class="line"><span>  status: OrderStatus</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>machine OrderLifecycle on Order.status {</span></span>\n<span class="line"><span>  allow transition Draft -&gt; Active</span></span>\n<span class="line"><span>  deny transition Active -&gt; Draft</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>The enforcement path is:</p><ol><li>The <code>machine</code> declaration compiles into allowed and denied transition constraints in <code>architecture.o</code>.</li><li>Built-in or plugin extractors produce deterministic graph facts from source code.</li><li>Committed <code>.aglang/extractors/*.agq.yml</code> files match those graph facts and emit transition facts with query provenance.</li><li><code>aglc check</code> asserts definite transition facts into Z3 and reports <code>state_machine_violation</code> when a transition contradicts the machine.</li><li><code>aglc explain --arch architecture.o --project . --violation &lt;id&gt; --json</code> re-runs the selected scope and returns the repair-loop explanation for the stable violation id.</li></ol><p>Transition query files are auditable source artifacts. LLMs may help draft them when requested, but <code>aglc check</code> does not call an LLM. Extracted transitions without a resolved <code>from</code> state are warnings only and are not asserted into Z3.</p><p>A blocking JSON verdict includes the machine name, transition edge, source file, evidence, query id/version/file, graph fact id when available, stable violation id, and conflicting Z3 assertions.</p><h2 id="permission" tabindex="-1">Permission <a class="header-anchor" href="#permission" aria-label="Permalink to &quot;Permission&quot;">​</a></h2><p>Permissions describe role/action rules. They are formal when extractors provide definite authorization evidence; otherwise they remain agent-visible intent.</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>enum Role { Admin | Member }</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>permission ProjectAccess on Project {</span></span>\n<span class="line"><span>  allow Role.Admin -&gt; *</span></span>\n<span class="line"><span>  deny Role.Member -&gt; delete</span></span>\n<span class="line"><span>}</span></span></code></pre></div><h2 id="data-and-enum" tabindex="-1">Data And Enum <a class="header-anchor" href="#data-and-enum" aria-label="Permalink to &quot;Data And Enum&quot;">​</a></h2><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data User {</span></span>\n<span class="line"><span>  classification: pii</span></span>\n<span class="line"><span>  jurisdiction: eu</span></span>\n<span class="line"><span>  id: UUID</span></span>\n<span class="line"><span>  email: String</span></span>\n<span class="line"><span>  roles: List&lt;Role&gt;</span></span>\n<span class="line"><span>}</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>enum Role { Admin | Member }</span></span></code></pre></div><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><p>Import another <code>.ag</code> file to compose large specs:</p><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import &quot;./shared/base.ag&quot;</span></span>\n<span class="line"><span>import &quot;./services/auth.ag&quot;</span></span></code></pre></div><p>Cyclic imports are detected and rejected at compile time.</p><h2 id="comments" tabindex="-1">Comments <a class="header-anchor" href="#comments" aria-label="Permalink to &quot;Comments&quot;">​</a></h2><div class="language-ag vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ag</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// Single-line comment</span></span></code></pre></div>', 77)
  ])]);
}
const languageReference = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  languageReference as default
};
